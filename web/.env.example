# 高德地图(可选):web 端 JS API Key。从 https://console.amap.com 申请「Web端(JS API)」。
# 没有也能跑——前端会自动降级为「文字方位」。把值填到下方并去掉行首注释,或新建 web/.env.local。
VITE_AMAP_KEY=
# 2021 后申请的 key 多数还需「安全密钥」,有就填(没有留空):
VITE_AMAP_SECURITY=
