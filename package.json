{
  "name": "chisha-liuyao",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "吃啥六爻 · 玄学餐厅推荐",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev -w server\" \"npm:dev -w web\"",
    "build": "npm run build -w server && npm run build -w web",
    "lint": "npm run lint -w server && npm run lint -w web",
    "start": "node --env-file=.env server/dist/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
